#Instalando pacotes NPM
- npm i moment 
- Pacotes instalados pelo npm ficam dentro do node_modules que apresenta os códigos desses módulos de terceiros instalados.
- Dentro do package.json na propriedade 'dependencies' apresentará os pacotes instalados para serem usados pelo require;
